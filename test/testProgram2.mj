program P
const int size = 10;
const bool isTrue = true;
const char robi = "R";
char lent;
const bool intBool = true;
int arr[];
bool boolArr[];
{


        int findMax(int arr[])
        int arrLen, maxNum;
        {
            if (arr == null)
                return -1;

            arrLen = len(arr);
            if (arrLen == 0)
                return 0;

            do{
                arrLen--;
                if (arrLen < 0)
                    break;

                if (maxNum < arr[arrLen])
                    maxNum = arr[arrLen];

            }while(arrLen >= 0);

            return maxNum;

        }

        void main()
        int arr[], i, maxNum;
        {
            arr = new int[10];

            i = 0;

            do
            {
                if (i == 10)
                    break;

                read(arr[i]);
            }while(i < 10);


            maxNum = findMax(arr);


            return;

        }

}
