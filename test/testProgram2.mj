program P
const int size = 10;
const bool isTrue = true;
const char robi = "R";
char lent;
const bool intBool = true;
int arr[];
{
        int helou(int c, int ck, int ckk)
        {

        }

        bool main(int k[], char c[])
        int x, y;
        {
            x = x + helou(size, size, size + size*size);
        }

}
